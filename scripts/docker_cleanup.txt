Write-Host "=== STEP 1: Docker cleanup (SAFE MODE, no volumes) ===" -ForegroundColor Cyan
docker system prune -a -f

Write-Host "=== STEP 2: Stopping Docker Desktop ===" -ForegroundColor Cyan
Stop-Process -Name "Docker Desktop" -Force -ErrorAction SilentlyContinue
Start-Sleep -Seconds 5

Write-Host "=== STEP 3: Shutting down WSL ===" -ForegroundColor Cyan
wsl --shutdown
Start-Sleep -Seconds 5

$VHDXPath = "C:\Users\ShanawazKhan\AppData\Local\Docker\wsl\disk\docker_data.vhdx"

Write-Host "=== STEP 4: Optimizing Docker VHDX ===" -ForegroundColor Cyan
Optimize-VHD -Path $VHDXPath -Mode Full

Write-Host "=== STEP 5: Restarting Docker Desktop ===" -ForegroundColor Cyan
Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"

Write-Host "=== DONE: Docker disk optimized safely ===" -ForegroundColor Green
