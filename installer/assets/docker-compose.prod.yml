# docker-compose.prod.yml
# Production stack — no source volume mounts, all secrets from .env
# Generated by the ezHealthKonnect installer.  Edit .env to reconfigure.
#
# Usage:
#   docker compose -f docker-compose.prod.yml up -d
#
# All ${...} variables are read from the .env file in the same directory.

services:

  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg15
    container_name: ezhk-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       ezhealthkonnect
      POSTGRES_USER:     ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_HOST_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ezhealthkonnect"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - ezhk

  # ── Flyway migrations ───────────────────────────────────────────────────────
  flyway:
    image: flyway/flyway:10-alpine
    container_name: ezhk-flyway
    environment:
      FLYWAY_URL:                  jdbc:postgresql://postgres:5432/ezhealthkonnect
      FLYWAY_USER:                 ${DB_USER}
      FLYWAY_PASSWORD:             ${DB_PASSWORD}
      FLYWAY_BASELINE_ON_MIGRATE:  "true"
      FLYWAY_CONNECT_RETRIES:      10
    volumes:
      - ./database/migrations:/flyway/sql:ro
    depends_on:
      postgres:
        condition: service_healthy
    command: ["repair", "migrate"]
    networks:
      - ezhk

  # ── MinIO object storage ────────────────────────────────────────────────────
  minio:
    image: minio/minio:latest
    container_name: ezhk-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER:     ${MINIO_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - ezhk

  # ── Ollama local LLM (optional — skip if AI_ENABLED=false) ─────────────────
  ollama:
    image: ollama/ollama:latest
    container_name: ezhk-ollama
    restart: unless-stopped
    profiles:
      - ai
    volumes:
      - ollama_data:/root/.ollama
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ezhk

  # ── Application ─────────────────────────────────────────────────────────────
  app:
    image: ${APP_IMAGE:-ghcr.io/ezinteropsolutions/ezhealthkonnect:latest}
    container_name: ezhk-app
    restart: unless-stopped
    environment:
      NODE_ENV: production

      # Database
      DB_HOST:     postgres
      DB_PORT:     5432
      DB_NAME:     ezhealthkonnect
      DB_USER:     ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_SSL:      ${DB_SSL:-false}

      # App
      PORT:     3000
      API_PORT: 8080

      # Secrets — generated by installer
      SESSION_SECRET: ${SESSION_SECRET}
      JWT_SECRET:     ${JWT_SECRET}

      # Object storage (MinIO)
      OBJECT_STORAGE_DRIVER:     s3
      OBJECT_STORAGE_BUCKET:     ezhealthkonnect
      OBJECT_STORAGE_REGION:     us-east-1
      OBJECT_STORAGE_ENDPOINT:   http://minio:9000
      OBJECT_STORAGE_PATH_STYLE: "true"
      OBJECT_STORAGE_ACCESS_KEY: ${MINIO_USER}
      OBJECT_STORAGE_SECRET_KEY: ${MINIO_PASSWORD}

      # AI / Ollama (only used when the ai profile is active)
      OLLAMA_HOST:        http://ollama:11434
      OLLAMA_CHAT_MODEL:  ${OLLAMA_CHAT_MODEL:-llama3.2:3b}
      OLLAMA_EMBED_MODEL: ${OLLAMA_EMBED_MODEL:-nomic-embed-text}

      # Schema package directory
      EZHEALTHKONNECT_SCHEMA_DIR: /app/schemas

      # Compliance
      HIPAA_COMPLIANCE_MODE: "true"
      GDPR_COMPLIANCE_MODE:  "true"

    ports:
      - "${APP_PORT:-3000}:3000"
      - "${API_PORT:-8080}:8080"
      # Note: internal Go port is always 8080; API_PORT maps it to the host
      # HL7 TCP/MLLP receiver ports
      - "2575:2575"
      - "6610:6610"
      - "6661-6670:6661-6670"
      # FHIR/HTTP receiver ports
      - "8081-8099:8081-8099"

    volumes:
      # Persistent data directories (not source code)
      - schemas_data:/app/schemas
      - logs_data:/app/logs
      - uploads_data:/app/uploads

    depends_on:
      flyway:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    networks:
      - ezhk

# ── Named volumes ───────────────────────────────────────────────────────────
volumes:
  postgres_data:
  minio_data:
  ollama_data:
  schemas_data:
  logs_data:
  uploads_data:

# ── Network ─────────────────────────────────────────────────────────────────
networks:
  ezhk:
    driver: bridge
